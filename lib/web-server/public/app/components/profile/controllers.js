(function(){
    'use strict';

    angular
        .module('components.profile')

        .controller('ProfileController', function($scope, $http, toastr, APP_CONFIG, apiService, auth, mySocket, notificationService){

        });
})();