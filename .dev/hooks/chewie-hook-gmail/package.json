{
  "name": "chewie-hook-gmail",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maxime Bret",
  "license": "ISC",
  "chewie": {
    "configOptions": [
      {
        "name": "fbToken",
        "label": "Token Facebook",
        "description": "The hook needs a first token in order to work. As long as the hook is able to refresh it you don't have to update the value.",
        "type": "text",
        "required": false
      }
    ]
  },
  "dependencies": {
    "gmail-send": "^1.2.7",
    "googleapis": "^18.0.0"
  }
}
