{
  "hooks": {
    "client-web-server": {
      "config": {
        "port": 3000,
        "proxyServerPort": 3001,
        "ssl": {
          "activate": true,
          "key": "$chewie{resourceDir}/.ssh/server.key",
          "cert": "$chewie{resourceDir}/.ssh/server.crt"
        }
      }
    },
    "scenarios": true,
    "plugins": true
  }
}